# MKV-Convertor
A cross-platform desktop app for converting MKV container files into browser-friendly MP4 files. Uses Electron for local file access/UI and FFmpeg (via an npm wrapper) to remux or transcode media streams.
